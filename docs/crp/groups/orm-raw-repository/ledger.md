# Ledger — orm-raw-repository (결함 원장)

> 분류: **Fix**(계약 위반·고친다) / **Accept-out-of-scope** / **Wont-fix**. 심각도: CRIT / HIGH / MED / LOW.

| ID | Severity | 위반 계약 조항 | 결정 | Status | 근거 | 비고 |
|---|---|---|---|---|---|---|
| F-001 | MED | C-7 (자식 프로세스 stdio UTF-8) | Fix | Fixed | Round 1 · `tests/scripts/test_new_app.py` | 자식 Python 이 로컬 코드페이지로 한글 stderr 출력 → 부모의 UTF-8 strict 디코딩이 터져 `proc.stderr` 가 **None**. `-X utf8` + `errors="strict"` + 대상 디렉터리 미생성 검증. fail-on-revert 확인 |
| F-002 | MED | C-7 | Fix | Fixed | Round 1 · `test_core_independence` · `test_admin_lazy_loading` · `test_db_router_env` | 같은 결함의 형제 3곳. 출력이 ASCII 인 동안만 우연히 통과하던 상태. `test_python_subprocesses_in_tests_force_utf8` 로 재발 차단 |
| F-003 | **HIGH** | C-6 (전용 인스턴스 격리) | Fix | Fixed | Round 1 · `compose.test.yaml` · `tests/integration/conftest.py` | 포트만으로 격리하면 sibling 저장소 컨테이너에 **조용히 붙어 스키마를 지운다**(실제 발생). 포트 3309 + DB·계정명을 저장소 전용으로 분리해 오접속 시 인증 거부로 드러나게 함 |
| F-004 | **HIGH** | C-5 (secret 로그 유출 금지) | Fix | Fixed | Round 1 · `app/utils/logs/filters.py` · `config.py` | 기본 `DEBUG=true` 에서 SQLAlchemy·드라이버가 bind 값을 그대로 출력. `SqlNoiseFilter` 를 **전 handler** 에 부착 + `LOG_SQL_ECHO_ENABLED` 스위치. 실제 canary probe 로 유출/차단 양방향 확인 |
| F-005 | MED | 계약: alembic 실행이 앱 로깅을 죽이지 않는다 | Fix | Fixed | Round 1 · `migrations/env.py` | `fileConfig()` 기본값이 기존 로거를 전부 비활성화. `disable_existing_loggers=False` + 자식 프로세스 생존 테스트 |
| F-006 | **HIGH** | 계약: `.env.example` 은 그대로 복사해 기동 가능해야 한다 | Fix | Fixed | Round 1 · `.env.example` | `CORS_ALLOW_ORIGINS=["*"]` + `CORS_ALLOW_CREDENTIALS=true` 조합을 config validator 가 거부 → **예제를 복사하면 기동 실패**. credentials 를 false 로 내리고 활성화 절차를 주석에 명시 |
| F-007 | **HIGH** | 계약: 인증 없는 `/admin` 이 사고로 공개되지 않는다 | Fix | Fixed | Round 1 · `config.py` | 개발 기본값 `ADMIN=true` 를 그대로 배포하면 무인증 관리자 화면 공개. 기본값은 유지(2026-08-12 결정)하고 production/staging 에서 `ADMIN_UNAUTHENTICATED_ACK` 미승인 시 **기동 거부** |
| F-008 | MED | C-7 | Fix | Fixed | Round 1 · `.github/workflows/ci.yml` | bandit 이 한글 리포트 출력 중 cp949 `UnicodeEncodeError` 로 크래시 — 게이트가 실패를 보고하려다 죽는 상태. workflow 전역 `PYTHONIOENCODING/PYTHONUTF8` 고정 |
| F-009 | MED | C-8 (문서 참조 실재) | Fix | Fixed | Round 1 · `README.md` · `ARCHITECTURE.md` · `QUICKSTART.md` | 존재하지 않는 `import_all_models()`, `main.py` 직접 router 등록, 기능 `__init__.py` 의 Router 재노출 안내가 **현재 절차**로 남아 있었음. 회귀 검사 6종 추가 |
| F-010 | **HIGH** | C-12 (500 응답 불투명) | Fix | Fixed | Round 2 · `app/core/bootstrap.py` | `detail = str(exc) if app_settings.DEBUG else None` — DB 예외의 `str()` 에는 SQL·bind 값·DSN 이 들어간다. DEBUG 서버가 노출되는 사고는 흔하다. `detail=None` 고정 + TestClient 실응답 canary 검증 |
| F-011 | **HIGH** | C-12 (예외 detail) | Fix | Fixed | Round 2 · `app/core/repositories/repository_base.py` | `detail={"error": str(e.orig)}` 가 **응답으로 나간다** — 중복키 오류 하나로 실행 SQL 과 바인딩 값이 API 클라이언트에 전달되던 경로. 4곳 제거 |
| F-012 | MED | C-13 (로그 예외 메시지 보간) | Fix | Fixed | Round 2 · repository_base · session.py · bootstrap.py · user_info_middleware.py | `logger.error(f"...: {e}")` 11곳. 로거 이름이 `app.*` 라 `sql_noise` 를 그대로 통과 — 필터로 막은 값이 옆문으로 나가던 상태. 타입만 남기도록 전환 |
| F-013 | MED | C-14 (SQL echo 환경 제한) | Fix | Fixed | Round 2 · `config.py` | `LOG_SQL_ECHO_ENABLED` 는 `LogSettings`, `ENV` 는 `AppSettings` 에 있어 어느 한쪽 validator 로도 잡히지 않았다. 교차 설정 검증 추가 |
| F-014 | MED | C-15 (loopback publish) | Fix | Fixed | Round 2 · `compose.test.yaml` | 포트만 적으면 docker 가 `0.0.0.0` 에 연다 — 같은 네트워크의 다른 기기가 테스트 DB 에 접속 가능. `127.0.0.1:` 바인딩 |
| F-015 | MED | C-5 (secret 로그 유출) | Fix | **Fixed** (Round 5) | Round 2 · `app/core/bootstrap.py:general_exception_handler` | `logger.exception` 의 traceback 마지막 줄에 DB 예외 메시지(SQL·bind 값)가 남는다. traceback 을 끄는 것은 해법이 아니다(ADR-010) — Round 5 에서 `convert_db_error()` + `from None` 으로 닫았다. 500 핸들러도 DB 예외에는 traceback 을 남기지 않는다. canary 가 최종 formatter 출력에 나타나지 않음을 확인 — **C-5 완전 충족** |
| F-016 | **HIGH** | C-16 (실패/정상 동일 cleanup) | Fix | Fixed | Round 3 · `app/core/resources.py` | 기존 lifespan 은 **정상 종료에서만** drain·dispose 했다. startup 이 깨지면 아무것도 닫지 않고 예외만 올라가 커넥션이 샜다. `AsyncExitStack` 으로 두 경로를 하나로 합치고, cleanup 실패가 뒤따르는 해제를 막지 않도록 격리 |
| F-017 | MED | C-5 이웃 (닫힌 pool 접근) | Fix | Fixed | Round 3 · `background_tasks.drain()` | drain timeout 후 pending 태스크를 **그대로 두고** 반환했다. 곧이어 engine dispose 가 돌면 살아남은 태스크가 이미 닫힌 pool 을 만진다. 취소 + gather 회수 + 추적 집합 비우기로 전환. 예산도 대기 80% / 회수 20% 로 분리(ADR-012) |
| F-018 | MED | C-2·C-3 (registry 가 유일한 진실) | Fix | Fixed | Round 3 · `create_db_tables()` | 전역 `Base.metadata.create_all` 은 registry 에 없는 모델(테스트가 정의한 임시 모델 포함)까지 만든다 — 설치하지 않은 앱의 테이블이 개발 DB 에 생기던 경로. 소유 테이블만 `tables=` 로 한정하고 0개면 접속조차 하지 않는다 |
| F-019 | MED | C-1 (쓰기는 writer 세션) | Fix | Fixed | Round 3 · `app/features/*/dependencies/` | 기능 쓰기 경로가 routed 세션을 쓰고 있었다. routed 는 첫 SELECT 가 replica 로 샐 수 있어, 방금 쓴 것을 다시 읽으면 복제 지연만큼 과거를 본다. `get_writer_db_session` 으로 고정하고 AST 검사로 재발 차단 |
| F-020 | MED | C-19 (컬럼 배치 고정) | Fix | Fixed | Round 4 · `app/core/models/models_base.py` | declarative mixin 의 컬럼이 모델 자신의 컬럼 **뒤로** 밀려 `id` 가 중간에 끼었다. Alembic 은 컬럼을 이름으로 비교해 순서를 diff 로 잡지 않으므로 `alembic check` 는 통과한다 — `create_all`(개발)과 migration(운영)의 컬럼 순서가 **아무 경고 없이** 갈리던 상태. `sort_order` 로 고정 + 회귀 테스트. fail-on-revert 확인(5건 실패) |
| F-021 | LOW | C-21 (rowcount 로 존재 판단 금지) | Fix | Fixed | Round 5 · `repository_base.update()` | `rowcount == 0` 을 "행이 없다"로 읽고 있었다. MySQL 서버 기본 동작은 **값이 바뀐** 행 수라, 값이 그대로인 PATCH 가 존재하는 리소스에 404 를 돌려줄 수 있다. **실측 결과 현재 스택(aiomysql, CLIENT_FOUND_ROWS)에서는 1 이 나와 지금 당장 버그는 아니었다** — 드라이버·플래그가 바뀌면 조용히 뒤집히므로 조회로 판단하도록 바꿨다 |

| F-022 | **HIGH** | C-23 (Raw DML 라우팅) | Fix | Fixed | Round 7 · `app/core/db/router.py::_is_write()` | `_is_write()` 가 ORM flush 와 Core `UpdateBase` 만 판별해 **`text("UPDATE ...")` 가 read-only 세션을 그대로 통과**하고 replica 로도 갈 수 있었다. requirements RAW-REP-007 이 default 프로젝트의 잔여 위험으로 명시한 항목이며, 이 저장소에도 같은 상태로 남아 있었다. SQL 문자열을 파싱하는 대신 호출부가 붙인 intent 로 판정하고, intent 없는 `TextClause` 는 쓰기로 본다(ADR-017). fail-on-revert 확인 — 두 방어선을 각각 되돌리면 **13건** 실패(일반 DML·소문자·선행 공백·주석·블록 주석·CTE DML·INSERT·UPDATE·미분류 우회·잠금 읽기 포함) |
| F-023 | LOW | C-24 (`query_name` 형식) | Fix | Fixed | Round 7 · `raw_repository_base.QUERY_NAME_PATTERN` | 정규식 끝을 `$` 로 잠갔더니 `"sales.daily
"` 이 통과했다 — `$` 는 문자열 끝의 개행 **앞**에서도 맞는다. 그 개행은 로그 한 줄을 둘로 쪼개 파서를 속인다. `\A...\Z` 로 교체. **테스트를 먼저 쓴 덕에 구현 직후 잡혔다** |
| F-024 | MED | 테스트 격리 | Fix | Fixed | Round 8 · `tests/integration/test_mysql_migration_roundtrip.py` | `from config import db_settings` 로 잡아 둔 참조를 monkeypatch 하면, 다른 테스트가 `importlib.reload(config)` 를 부른 뒤에는 **조용히 무시된다** — 모듈 속성이 새 객체로 바뀌고 `migrations/env.py` 는 실행 시점에 그 새 객체를 읽기 때문이다. 그래서 alembic 이 테스트 DB 가 아니라 개발 DB 자격증명으로 붙어 1045 로 실패했다. 단독 실행은 통과하고 전체 실행만 실패해 순서 의존이었다. 호출 시점의 모듈 속성을 패치하도록 수정 |
| F-025 | MED | C-27 (schema 이름) | Fix | Fixed | Round 9 · `auth_schema.py` | `UserResponse` 가 auth·user 두 모듈에 있어 OpenAPI 가 `app__features__auth__schemas__auth_schema__UserResponse` 를 내보내고 있었다. 그 key 는 **파일을 옮기면 바뀌므로** 이 스키마로 생성한 클라이언트가 통째로 깨진다. 의미가 좁은 auth 쪽을 `AuthenticatedUserResponse` 로 개명(ADR-021). 결과(`__` 금지)와 원인(이름 고유성) 두 규칙으로 재발 차단 |
| F-026 | LOW | C-28 (태그 정합) | Fix | Fixed | Round 9 · `tags_metadata.py` | `Auth` 태그를 쓰는데 선언이 없어 Scalar 에 설명 없이 나타났고, 반대로 `Analytics` 는 선언만 있고 쓰이지 않아 빈 항목으로 남았다. 구현이 끝난 Blog·User·Reply·SNS 설명에는 “미구현/예정”이 그대로 남아 있었다 — 문서가 “예정”이라 말하는 기능을 이미 호출할 수 있는 상태다. 전부 실제 라우트 기준으로 다시 씀 |
| F-027 | LOW | C-12 이웃 (문서 품질) | Fix | Fixed | Round 9 · `app/core/exception.py` | `ErrorResponse` 가 OpenAPI **3.0** 문법(`example` 단수)을 쓰고 있었다. 이 앱은 3.1 을 내보내므로 도구에 따라 조용히 무시된다. 모든 오류 응답에 나오는 모델이라 여기서 틀리면 예시가 전부 사라진다. `examples`(배열)로 전환하고 필드 설명 추가 |
| F-028 | MED | C-29 (규칙이 실제로 도는가) | Fix | Fixed | Round 9 · `tests/test_openapi_contract.py` | 처음 작성한 규칙 중 셋이 **절대 실패할 수 없는 장식**이었다. (1) `summary` 가 비었는지 — FastAPI 가 함수명으로 자동 생성하므로 항상 참, (2) `operationId` 가 비었는지 — 같은 이유, (3) 성공 응답의 `content` 유무 — `response_model` 을 지워도 반환 타입 어노테이션에서 schema 가 다시 만들어진다. 더해서 `app.routes` 직접 순회가 FastAPI 0.141 의 `_IncludedRouter` 때문에 기능 라우트를 **하나도 못 보고** 있었다(3개만 보였다). fail-on-revert 를 돌리지 않았으면 전부 초록불로 남았을 것 |
| F-029 | **HIGH** | C-31 (게이트 병렬 안전) | Fix | Fixed | Round 10 · `scripts/openapi_revert_check.py` | fail-on-revert 하네스는 소스를 고쳤다 되돌린다. 두 판이 겹치면 A 가 주입한 상태를 B 가 “원본” 으로 백업하고, A 가 복구한 뒤 B 가 그 백업을 다시 써서 **주입된 문구가 저장소에 남는다**. `finally` 복구로는 못 막는다 — 겹치는 구간이 복구 이전이다. 게이트 3개를 동시에 돌려 `tags_metadata.py` 에 `(예정)` 이 남는 것을 실측했다. temp 기반 배타 락으로 순차화(거절이 아니라 대기) |
| F-030 | MED | C-31 (게이트 병렬 안전) | Fix | Fixed | Round 10 · `tests/scripts/test_new_app.py` | 이 테스트는 설계상 실제 `app/features/` 아래에 앱을 만든다(생성물이 절대 경로를 참조해 임시 디렉터리로는 검증이 안 된다). 이름이 고정이라 병렬 실행 시 두 판이 같은 디렉터리를 다투고 한쪽이 “이전 실행의 잔해” 로 오판해 실패했다. 이름에 PID 를 붙여 갈랐다 |
| F-031 | MED | C-30 (문서 참조 실재) | Fix | Fixed | Round 10 · `README.md` · `docs/ARCHITECTURE.md` | 문서 **21곳**이 Phase 7 에서 지운 세션 Dependency 옛 이름을 들고 있었고, 별도로 **5곳**이 Phase 3b 에서 제거한 Repository 메서드(`get_by_id_with`·`get_all_with`·`bulk_create`)를 안내하고 있었다. 따라 하면 `ImportError`/`AttributeError` 가 난다. 코드 테스트는 전부 초록불이었다 — 문서는 아무것도 import 하지 않는다. 더해서 쓰기 예시가 routed 세션을 쓰고 있었다(F-019 에서 코드는 writer 로 고정했음) — 이름뿐 아니라 **어떤 dependency 를 쓰는지**도 틀렸다 |
| F-032 | **HIGH** | C-33 (Playwright async) | Fix | Fixed | Round 11 · `tests/browser/conftest.py` | `playwright.sync_api` 를 쓰자 브라우저와 **무관한** 테스트가 38 failed / 124 errors 로 깨졌다(`RuntimeError: This event loop is already running`). sync API 가 자기 이벤트 루프를 돌리는데 이 저장소는 `asyncio_mode=auto` 라 pytest-asyncio 가 같은 스레드에서 루프를 돌린다. **게이트는 마커로 나눠 돌려서 통과했다** — 마커 없이 전체를 돌린 뒤에야 드러났다. async API 로 전환 |
