"""공용 검증 상수.

여러 도메인 스키마가 공유하는 최소 형식 검증 규칙을 한곳에 둔다(중복 정의 방지).
"""

# 외부 email-validator 의존 없이 최소 형식만 확인하는 이메일 정규식.
# (local@domain.tld 형태의 공백/@ 없는 세 토큰만 강제)
EMAIL_PATTERN = r"^[^@\s]+@[^@\s]+\.[^@\s]+$"
