# Run Log — learning-path

> 라운드별 실행 기록. 무엇을 했고, 무엇으로 확인했는지.

## Round 0 — 검수 (2026-08-20, `3cbb354`)

사용자 요구("학습 가능한가")를 측정으로 환산했다. 진입 문서 3종에서 개념 키워드 빈도를
세고, `project-guide` 와 코드의 최종 갱신 시각을 대조했다.

결과: Django 스타일 ✅ / ORM·Raw ❌. 결함 L-001~L-006 기록, `audit-report.md` ·
`charter.md` 작성.

## Round 0.5 — 재검수 (2026-08-20, `b88f654`)

컨텍스트 compact 후 요약을 믿지 않고 저장소에 다시 물었다. Round 0 결론은 그대로였으나
두 건이 더 나왔다.

- **L-007** — `project-guide` 의 세션 API 3개가 코드에 0회. 뒤처짐이 아니라 오류
- **L-008** — `BASE_DOCS` 가 진입 문서 3종뿐이라 `project-guide` 가 검사 범위 밖

처방을 추측하지 않고 프로브로 증명: `BASE_DOCS` 임시 확대 → **9 failed / 31 passed**.
그중 3건만 진짜 결함(죽은 심볼), 6건은 헛통과 방지 가드(가이드 문서가 루트 기준 경로를
안 씀). 이 구분을 기록해 다음 세션이 6건을 결함으로 오해하지 않게 했다. 프로브 복원 후
13개 통과 확인.

착수 순서를 `T-4a → T-1 → T-2 → T-3` 으로 재배열(ADR-L03).

## Round 1 — 2026-08-20 (base SHA: `b88f654`) — T-4a → T-1 → T-2 → T-3

권장 순서(ADR-L03)를 그대로 따랐다. 커밋 3개.

### 선행 — 환경

이 저장소에는 `.env` 가 없고 전용 테스트 컨테이너(`fastapi-passive-mysql-test`, 3309)도
꺼져 있었다. 그 상태에서 게이트 8단계 중 7·8번이 통과할 수 없다 — browser 검사는 실제
uvicorn 을 띄우는데 개발 모드 startup 이 DB 에 붙기 때문이다(당시 7 errors / 15 skipped).
컨테이너만 띄우면 해소됐다. browser 하네스가 자격증명을 통합 테스트 하네스에서 직접
주입하므로 `.env` 는 필요 없다 — 만들었다가 되돌렸다. 설정 입력을 늘리면 회귀 표면만 는다.

### T-4a + T-1 — `9acbad8`

T-4a 는 이전 세션이 워킹트리에 구현해 둔 상태였고, 그 검사가 짚어준 빨간불 2건이 그대로
T-1 의 작업 목록이었다 — ADR-L03 이 의도한 대로다.

- `v1.1/README.md` 에 루트 기준 경로가 하나도 없어 헛통과 가드가 걸렸다. 열어보니 **v1.0
  사본 그대로**였다 — 문서 버전 1.0.0, 대상 프로젝트가 형제 저장소, 기준 커밋 `9c93803`,
  그리고 ORM/Raw 를 "아직 구현되지 않은 향후 계획" 으로 소개하고 있었다. L-002 가 인덱스에
  남아 있던 셈이다. 인덱스를 다시 썼다.
- `DATE_ADD` 가 환경변수로 오인됐다. 07 문서가 "SQL 이 아니라 Python 에서 날짜를 계산하는
  이유" 로 인용하는 MySQL 함수다. T-4a 의 SQL 키워드 allowlist 에 빠져 있었다.
- `09-orm-vs-raw-decision.md` 신설(L-004). 09 를 추가하자 검사 케이스가 40 → 43 으로 늘었다
  — 최신 버전 폴더 자동 편입이 **동작한다는 증거**다.

### T-2 — `d74ab5a`

세 진입 문서 어디에도 `RawRepositoryBase` 가 0회, `project-guide` 링크가 0회였다.
README 에 `## ORM / Raw 데이터 접근` 을 신설하고, ARCHITECTURE 는 Repository 상속 대상을
두 계열로 서술하고, QUICKSTART 는 골격 생성 직후의 갈림길로 안내하게 했다.

제목 잔재(L-005)를 고치다 **같은 잔재가 세 군데 더** 있는 것을 봤다 — 구조 트리 루트,
clone URL, 그리고 v1.1 문서 01~08 의 메타데이터. 제목만 고치면 심화 문서에는 형제 저장소
이름이 그대로 남는다. 한 번에 정리했다.

**게이트가 흔들려 멈췄다(L-010).** browser 2건이 실패했는데 이 그룹은 앱 코드를 건드리지
않았다. 변경을 stash 하고 같은 검사를 돌리자 **그 상태에서도 실패**했다 — 이 그룹의 회귀가
아니었다. 원인은 고정 대기였다: Scalar 는 태그 섹션을 지연 렌더링하는데 검사가 networkidle
이후 2.5초만 기다린다. 한가할 때만 맞는 대기다. 기대 문자열이 나타날 때까지 폴링하도록
바꾸고(250ms 간격, 20초 상한) 단정은 그대로 뒀다. 3연속 통과.

### T-3 — `7b907cf`

`REQUIRED_IN_ENTRY_DOCS` 로 도달 지점 5종에 **항목별 최소 등장 문서 수**를 못박았다
(ADR-L05). 버전 드리프트(ADR-L06)·README 제목·구조 트리 검사를 더했다.

fail-on-revert 를 5종으로 실증했고, **여섯 번째가 통과해버렸다**: 구조 트리에서
catalog·reports 를 지워도 초록이었다. 검사가 README 본문 전체를 보고 있어서 다른 절의
`app/features/catalog/...` 경로가 조건을 대신 만족시켰기 때문이다. 트리 블록만 보도록
좁혀 다시 증명했다. **검사를 쓰고 되돌려보지 않았으면 이 헛통과를 몰랐다** — 이 그룹이
고치려던 결함(검사가 보지 않는 곳)이 새 검사에서 그대로 재현된 셈이다.

### 게이트 결과

`review_gate.py` 8단계 전부 통과 — ruff format · ruff check · mypy · bandit ·
pytest(단위+불변식+공개 API+OpenAPI) · OpenAPI fail-on-revert · `-m mysql`(skip 금지 22건) ·
`-m browser` 7건. 문서 검사는 13 → 51 케이스.

### 수렴 판정

`CONVERGED` — Open Fix 0. L-001~L-008 Fixed, Round 1 중 발견한 L-009·L-010 도 같은
라운드에 Fixed, L-011 은 계약 밖이라 RL-05 로 수용.

**단 하나 남은 것**은 인수 기준의 마지막 줄 — "README 만 읽고 Raw 기능을 하나 만들 수
있는가" 다. RL-03 이 명시한 대로 자동화할 수 없는 **사람 판정**이라 사용자 확인을 기다린다.

## 심각도 추세 (수렴이 보이게)

| Round | CRIT | HIGH | MED | LOW | 신규 | 판정 |
|---|---|---|---|---|---|---|
| 0 | 0 | 2 | 2 | 2 | 6 | NOT CONVERGED (Open Fix 6) |
| 0.5 | 0 | 2 | 0 | 0 | 2 | NOT CONVERGED (Open Fix 8) |
| 1 | 0 | 0 | 1 | 2 | 3 (L-009·L-010 Fixed / L-011 Accepted) | **CONVERGED** (Open Fix 0) |

## Round 1.1 — 2026-08-20 (base SHA: `95a7428`) — 문서 정합성 정정

- **트리거:** 푸시 후 잔여 작업 확인. 미닫힘 체크박스를 훑다가 `audit-report.md` §5 의
  **열 칸이 전부 미체크**인 것을 발견 — Round 1 의 `CONVERGED` 선언과 표면상 모순이다.
- **원인:** Round 1 마감 때 `checklist.md` 만 닫고 `audit-report.md` 를 보지 않았다.
  charter §4 가 "인수 기준은 audit-report §5 를 그대로 쓴다" 고 선언했으므로 그쪽이 정본이다.
  같은 그룹에 체크박스 파일이 둘인데 한쪽만 닫은 상태였다.
- **재측정:** 아홉 칸 전부 실질 충족이었다. Raw 등장 3/3 문서 · 구조 트리 실물 확인 ·
  09 문서 실재 · 가이드 링크 3·2·2회 · v1.1 죽은 세션 API 0회 · BASE_DOCS 13문서 ·
  fail-on-revert 5종 · 게이트 8/8(CI run `32335993433` 2 job success).
- **닫지 않은 것 1건:** 학습자 시나리오 수동 검증. RL-03 이 자동화 불가로 명시한 사람
  판정이라 기계가 닫지 않는다. 근거 없이 체크하면 이 문서 전체의 신뢰가 떨어진다.
- **함께 관측(다른 그룹, 이번에 고치지 않음):** `orm-raw-repository/charter.md` §3 의
  인수 기준 8칸도 미체크인데 그 그룹의 `completion-report.md` 는 "열린 결함 0건" 이라고
  적혀 있다. 같은 종류의 어긋남이다. 근거는 그 그룹 run-log 에 있으나(594 passed 기록,
  mysql 실행, 게이트 8단계) **INV-1~6 구조 증거 매핑을 한 건씩 대조해야** 닫을 수 있어
  이 라운드에서 손대지 않았다 — 남의 그룹을 근거 없이 체크하지 않는다.
- **변경:** `audit-report.md` 만 수정. 코드·테스트 diff 0.
- **수렴 판정:** `CONVERGED` 유지 (Open Fix 0). 신규 결함 0, 문서 정합성 정정 1.

## Round 1.2 — 2026-08-20 (base SHA: `ed86e57`) — flake 재발과 진짜 원인

- **트리거:** 다른 그룹(`orm-raw-repository`) 문서 정정 중 게이트가 다시 빨개졌다.
  Round 1 에서 "고쳤다" 고 기록한 바로 그 검사다.
- **관측:** 실패 본문에 `Sales Reports … /api/v1/reports/daily-sales … Show More` 가
  **들어 있었다.** 섹션은 그려졌는데 요약문만 없었다. 즉 지연 렌더링이 아니다.
- **진짜 원인:** Scalar 는 항목이 많으면 `Show More` 뒤로 접고, 접힌 동안 경로는 나오지만
  요약문은 DOM 에 존재하지 않는다. **기다려서 풀리는 상태가 아니다.** Round 1 의 폴링
  수정은 원인의 절반(지연 렌더링)만 덮었고, 나머지 절반에서는 20초를 채우고 같은 실패를
  냈다. 3연속 통과는 마침 펼쳐진 채로 그려졌던 것이다.
- **조치:** 기대 문자열이 안 보이면 보이는 `Show More` 를 눌러 펼친 뒤 다시 본다.
  단정은 그대로여서 문자열이 정말 없으면 여전히 실패한다. **5연속 통과** 확인.
- **교훈:** "3연속 통과" 는 간헐 결함의 부재 증명이 아니다. 실패 메시지에 이미 답이
  들어 있었는데(경로는 있고 요약만 없음) 첫 수정 때 그걸 읽지 않고 흔한 원인(타이밍)으로
  단정했다.
- **수렴 판정:** `CONVERGED` 유지 (Open Fix 0). L-012 는 같은 라운드에 Fixed.

## Round 1.3 — 2026-08-20 (base SHA: `ed86e57`) — 세 번째 층

- **트리거:** L-012 를 고친 뒤 단독 5연속 통과했는데 **게이트 안에서만** 또 실패했다.
- **가른 방법:** 게이트가 쓰는 명령을 그대로 재현했다 — `-o addopts=` 로 기본 옵션을 지운
  채 돈다. 재현하니 실패 지점이 달랐다: `assert 102 > 1000`, 즉 페이지가 **아예 안 그려진**
  상태를 읽었다. 앞선 두 번(지연 렌더링·접힘)과 또 다른 층이다.
- **원인:** `docs_page` 픽스처가 `networkidle` 까지만 기다린다. 네트워크가 조용해지는 것과
  화면이 칠해지는 것은 다르다 — JS 가 받아온 스키마로 DOM 을 만드는 동안 네트워크는 조용하다.
  픽스처 docstring 은 "Scalar 가 그려질 때까지 기다린 페이지" 라고 **약속**하는데 코드가
  그 약속을 지키지 않았다.
- **조치:** 본문 길이가 1,000자에 이를 때까지 폴링(20초 상한). 기다리기만 하고 단정하지
  않는다. 게이트 동일 명령 3연속 + 전체 게이트 2연속 통과.
- **세 층 정리:** ① 지연 렌더링(L-010, 폴링) ② 접힘(L-012, Show More 펼치기)
  ③ 그리기 전 읽기(L-013, 픽스처 폴링). 같은 파일의 같은 증상이었지만 원인이 셋이었다.
  첫 두 번은 "재현되니 고쳤다" 로 끝냈는데, 재현 조건을 **게이트와 똑같이** 맞추고 나서야
  세 번째가 드러났다. 로컬에서 되는데 게이트에서 안 되면 명령줄부터 맞춰야 한다.
- **수렴 판정:** `CONVERGED` 유지 (Open Fix 0). L-013 은 같은 라운드에 Fixed.

