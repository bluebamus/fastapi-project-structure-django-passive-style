# Residual Risk — learning-path

> 이번 그룹에서 **닫지 않고 넘기는** 위험. 조건이 오면 다시 연다.

| ID | 위험 | 왜 지금 닫지 않나 | 다시 열 조건 |
|---|---|---|---|
| RL-01 | 문서 신선도는 **공개 심볼 이름**만 검사한다. 설명 내용이 틀린 것은 못 잡는다 | 자연어 정합성을 기계로 판정할 방법이 없다. 이름 검사만으로 L-007 같은 치명적 오류는 잡힌다 | 이름은 맞는데 설명이 틀린 결함이 실제로 보고되면 |
| RL-02 | `v1.0` 을 남겨두므로 검색에서 옛 문서가 먼저 걸릴 수 있다 | 버전 폴더 규약(ADR-L01). 지우면 이력이 사라진다 | 학습자가 v1.0 을 현행으로 오해한 사례가 나오면 → `v1.0/README.md` 에 deprecation 배너 |
| RL-03 | "README 만 읽고 Raw 기능을 만들 수 있는가" 는 **사람 판정**이다 | 자동화 불가. 체크리스트로 남긴다 | — |
| RL-04 | 검사가 진입 문서·`project-guide` 만 훑는다. `docs/orm-raw-repository/` 설계 문서는 여전히 범위 밖 | 설계 문서는 특정 시점의 결정 기록이라 현행 코드와 어긋나는 게 정상이다 | 설계 문서를 "현행 참조"로 쓰기 시작하면 |
| RL-05 | `pyproject.toml` 의 `name` 이 아직 `fastapi-default-project-structure` 다 (L-011) | 이 그룹의 diff 는 문서·테스트로 한정된다(INV-L1). `name` 을 바꾸면 `uv.lock` 재생성이 따라오고, 그건 공급망 고정을 건드리는 변경이라 문서 그룹이 함께 처리할 일이 아니다. 학습 경로에는 영향이 없다 — 진입 문서·가이드·구조 트리는 전부 정정됐다 | 패키징을 실제로 쓰기 시작하거나, `uv.lock` 을 어차피 재생성하는 작업이 생기면 그때 한 커밋으로 |
| RL-06 | browser 검사는 **20초 안에 그려진다**를 전제한다 (L-010) | 지연 렌더링을 기다리는 상한이 필요하고, 무한 대기는 CI 를 멈춘다. 20초는 관측된 렌더 시간(2~3초)의 여유 있는 배수다 | CI 에서 상한을 채우고 실패하는 일이 반복되면 — 그때는 대기를 늘리는 게 아니라 Scalar 렌더 경로를 봐야 한다 |

